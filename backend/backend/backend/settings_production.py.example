"""
Production settings template for Django backend.
Copy this to settings_production.py and update with your values.
Or use environment variables in your hosting service.
"""

from .settings import *
import os

# SECURITY: Use environment variable for secret key
SECRET_KEY = os.environ.get('SECRET_KEY', 'CHANGE-THIS-IN-PRODUCTION')

# SECURITY: Never run with DEBUG=True in production
DEBUG = os.environ.get('DEBUG', 'False') == 'True'

# Update with your actual domain
ALLOWED_HOSTS = [
    'your-app.railway.app',      # If using Railway
    'your-app.onrender.com',     # If using Render
    'your-app.herokuapp.com',    # If using Heroku
    'your-domain.com',            # Your custom domain
]

# CORS: Update with your frontend URL
CORS_ALLOWED_ORIGINS = [
    "https://YOUR_USERNAME.github.io",  # GitHub Pages
    "https://your-custom-domain.com",   # Custom domain
]

# Database: Use PostgreSQL in production
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': os.environ.get('DB_NAME', 'cashbook'),
        'USER': os.environ.get('DB_USER', 'postgres'),
        'PASSWORD': os.environ.get('DB_PASSWORD', ''),
        'HOST': os.environ.get('DB_HOST', 'localhost'),
        'PORT': os.environ.get('DB_PORT', '5432'),
    }
}

# Static files (if needed)
STATIC_ROOT = os.path.join(BASE_DIR, 'staticfiles')
STATIC_URL = '/static/'

# Security settings for production
SECURE_SSL_REDIRECT = True
SESSION_COOKIE_SECURE = True
CSRF_COOKIE_SECURE = True
SECURE_BROWSER_XSS_FILTER = True
SECURE_CONTENT_TYPE_NOSNIFF = True

